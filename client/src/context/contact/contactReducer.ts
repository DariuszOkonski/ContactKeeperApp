import { ActionType, InitialState } from '../../interfaces/general';

export const contactReducer = (
  state: InitialState,
  action: ActionType
): any => {
  switch (action.type) {
    default:
      return state;
  }
};
