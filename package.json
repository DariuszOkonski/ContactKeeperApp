{
  "name": "contactkeeperapp",
  "version": "1.0.0",
  "description": "MERN app",
  "main": "server.js",
  "scripts": {
    "server": "npm start --prefix server",
    "client": "npm start --prefix client",
    "setup:dep": "npm install && npm install --prefix client && npm install --prefix server",
    "start:dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "author": "Dariusz Okonski",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.0.1"
  }
}
